# 2026-04-07 Daily Log

## 20:45 - LiteSpeed Cron 排程錯誤修復

**問題發現**
- 韋瀚回報 hg01.org 再次狂送 LiteSpeed 錯誤郵件
- 問題：wp-cron 重新排程時，LiteSpeed 自訂排程 key「litespeed_filter」不存在
- 導致 hooks litespeed_task_imgoptm_* / _ccss / _ucss / _avatar 全部報 invalid_schedule
- 原因：殘留的 cron entries 參考到舊的自訂排程，wp-cron（CLI）啟動時外掛未正確掛上 schedule

**即時修復 hg01.org**
- 執行一次性修復腳本，批次取消所有 litespeed_task_* 事件
- 清掉 cron 陣列中的殘留項
- LiteSpeed 會在前台有流量時自行重建正確的排程

**擴大修復範圍**
- 韋瀚同意擴大到所有 WP 站點
- 範圍：31 個活躍的 WordPress 網站
- 結果：30 個成功清理並重建排程
- 例外：nasa11casino.com 執行時回報 WordPress 核心函式缺失

**nasa11casino.com 特殊處理**
- 問題：WordPress 核心載入失敗，站本身有壞檔/環境問題
- 採用 Plan A：暫停該站 wp-cron（在 wp-config.php 加入 DOING_CRON 判斷）
- 僅影響排程執行，不影響前台瀏覽
- 建議後續 02:00-03:00 執行完整修復（備份→修復核心→驗證→還原cron）

**技術細節**
- 修復腳本：清理 `_get_cron_array()` 和 `get_option('cron')` 中的 litespeed_task_* 項目
- 對被 Cloudflare 檔到的站點，改用源站 IP + Host 頭呼叫
- 執行後自動清理臨時 PHP 檔案

**預期結果**
- 所有站點不再發送 LiteSpeed cron 錯誤郵件
- 若 24 小時內還收到同樣主旨的信件，將針對該站再次處理

## 狀態追蹤

- **完成**: 30/31 站 LiteSpeed cron 排程修復
- **暫緩**: nasa11casino.com wp-cron 已暫停，等待深度修復
- **監控**: 觀察 24 小時內是否還有錯誤郵件
## Signal API 部署完成 ✅ (09:10-09:30)

### 部署成功
- **位置**：VPS 128.199.249.195:/opt/signal-api
- **端口**：8081（避免與 Caddy 8080 衝突）
- **容器狀態**：✅ 健康運行中
- **API 端點**：http://localhost:8081

### 部署問題解決
1. **端口衝突**：8080 被 Caddy 占用，改為 8081
2. **環境變數衝突**：移除 `AUTO_RECEIVE_SCHEDULE`（與 json-rpc 模式不相容）
3. **Docker Compose 版本**：VPS 使用 v2 需要 `docker compose`（空格）

### 註冊 Signal 號碼進行中
- **號碼**：+527711647956（墨西哥）
- **狀態**：需要 Captcha 驗證
- **步驟**：韋瀚需完成 https://signalcaptchas.org/registration/generate.html 驗證

### 整合架構確認
- **CS-Bot**：/opt/cs-bot/ + port 3000（客服，已上線）
- **Reporting-Bot**：/opt/pera57-reporting-bot/ + port 3001（回報，待部署）
- **Signal API**：/opt/signal-api/ + port 8081（通知 API，已部署）
- **完全隔離**：三個系統互不影響

### 下一步
1. 完成 Signal 號碼註冊（待 Captcha）
2. 建立 PERA57 回報群組
3. 測試 Signal 訊息發送
4. 設定 PERA57 網站連接資訊
5. 完整測試並部署回報機器人

---
*記錄時間：09:30*
*狀態：Signal API 部署完成，等待註冊完成*