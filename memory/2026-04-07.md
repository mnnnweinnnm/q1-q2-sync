# 2026-04-07 Daily Log

## 20:45 - LiteSpeed Cron 排程錯誤修復

**問題發現**
- 韋瀚回報 hg01.org 再次狂送 LiteSpeed 錯誤郵件
- 問題：wp-cron 重新排程時，LiteSpeed 自訂排程 key「litespeed_filter」不存在
- 導致 hooks litespeed_task_imgoptm_* / _ccss / _ucss / _avatar 全部報 invalid_schedule
- 原因：殘留的 cron entries 參考到舊的自訂排程，wp-cron（CLI）啟動時外掛未正確掛上 schedule

**即時修復 hg01.org**
- 執行一次性修復腳本，批次取消所有 litespeed_task_* 事件
- 清掉 cron 陣列中的殘留項
- LiteSpeed 會在前台有流量時自行重建正確的排程

**擴大修復範圍**
- 韋瀚同意擴大到所有 WP 站點
- 範圍：31 個活躍的 WordPress 網站
- 結果：30 個成功清理並重建排程
- 例外：nasa11casino.com 執行時回報 WordPress 核心函式缺失

**nasa11casino.com 特殊處理**
- 問題：WordPress 核心載入失敗，站本身有壞檔/環境問題
- 採用 Plan A：暫停該站 wp-cron（在 wp-config.php 加入 DOING_CRON 判斷）
- 僅影響排程執行，不影響前台瀏覽
- 建議後續 02:00-03:00 執行完整修復（備份→修復核心→驗證→還原cron）

**技術細節**
- 修復腳本：清理 `_get_cron_array()` 和 `get_option('cron')` 中的 litespeed_task_* 項目
- 對被 Cloudflare 檔到的站點，改用源站 IP + Host 頭呼叫
- 執行後自動清理臨時 PHP 檔案

**預期結果**
- 所有站點不再發送 LiteSpeed cron 錯誤郵件
- 若 24 小時內還收到同樣主旨的信件，將針對該站再次處理

## 狀態追蹤

- **完成**: 30/31 站 LiteSpeed cron 排程修復
- **暫緩**: nasa11casino.com wp-cron 已暫停，等待深度修復
- **監控**: 觀察 24 小時內是否還有錯誤郵件
## Signal API 部署完成 ✅ (09:10-09:30)

### 部署成功
- **位置**：VPS 128.199.249.195:/opt/signal-api
- **端口**：8081（避免與 Caddy 8080 衝突）
- **容器狀態**：✅ 健康運行中
- **API 端點**：http://localhost:8081

### 部署問題解決
1. **端口衝突**：8080 被 Caddy 占用，改為 8081
2. **環境變數衝突**：移除 `AUTO_RECEIVE_SCHEDULE`（與 json-rpc 模式不相容）
3. **Docker Compose 版本**：VPS 使用 v2 需要 `docker compose`（空格）

### 註冊 Signal 號碼進行中
- **號碼**：+527711647956（墨西哥）
- **狀態**：需要 Captcha 驗證
- **步驟**：韋瀚需完成 https://signalcaptchas.org/registration/generate.html 驗證

### 整合架構確認
- **CS-Bot**：/opt/cs-bot/ + port 3000（客服，已上線）
- **Reporting-Bot**：/opt/pera57-reporting-bot/ + port 3001（回報，待部署）
- **Signal API**：/opt/signal-api/ + port 8081（通知 API，已部署）
- **完全隔離**：三個系統互不影響

### 下一步
1. 完成 Signal 號碼註冊（待 Captcha）
2. 建立 PERA57 回報群組
3. 測試 Signal 訊息發送
4. 設定 PERA57 網站連接資訊
5. 完整測試並部署回報機器人

---
*記錄時間：09:30*
*狀態：Signal API 部署完成，等待註冊完成*
## 2026-04-07 上午

### Mistral API 問題解決
- **問題**：Mistral API key 401 錯誤，導致持續 fallback 到 Claude Sonnet（最貴模型）
- **發現**：`mistral-large-latest` 其實支援視覺功能（= `mistral-large-2512`），之前 fallback 純粹是 API key 無效
- **解決**：韋瀚提供新 API key `RvO7kdbpl8bFwt9mZ11sD6LSqYLihK0f`，驗證成功
- **Fallback 順序調整**：Mistral → Claude Opus → GPT-5 → Claude Sonnet（最強優先，最貴最後）

### 切換到 DeepSeek
- **原因**：韋瀚覺得 Mistral 理解力差，影響回覆機器人專案
- **DeepSeek 優勢**：價格便宜（$0.28/$0.42 per M），中文能力強，工具調用支援好
- **API key**：`sk-ff8abb433b7d48a2b4bc72646ce98ec8`（有效）
- **配置**：預設模型改為 `deepseek/deepseek-chat`，圖片模型仍用 Claude Sonnet（DeepSeek 不支援圖片）

### GitHub 同步設定完成
- **Repo**：`mnnnweinnnm/q1-q2-sync`（private）
- **同步方式**：每 5 分鐘 cron 自動 push/pull
- **排除**：`IDENTITY.md` 各自獨立，不同步
- **脫敏**：移除 `memory/2026-04-06.md` 中的 Google OAuth secret 後推送
- **狀態**：Q1 ↔ GitHub ↔ Q2 雙向同步正常，不再依賴同一個 WiFi

### 教訓
1. **不要亂改 config**：在 `openclaw.json` 加不存在的 `browser` key 導致 gateway 無法啟動
2. **API key 要直接測試**：用 curl 測試 API key 有效性，不要只依賴 OpenClaw 的錯誤訊息
3. **圖片模型配置**：DeepSeek 不支援圖片，需保留 Claude Sonnet 作為圖片模型
4. **GitHub secret 掃描**：推送前先脫敏敏感資訊，避免被阻擋

---

## PERA57 回報機器人開發 (14:30-17:13)

### 已完成
1. **PERA57 後台登入成功**
   - URL: `https://www.123bet3bo.com/`
   - 帳號: `peraaitest` / 密碼: `Aa085168@`
   - 需切換為「密碼驗證」模式（非 Google 驗證碼）

2. **後台功能完整探索 (16 個模組)**
   - 詳細筆記已存入 `memory/projects/pera57-backend-notes.md`
   - 核心數據來源：首頁運營概括、報表管理（平台損益總表）、玩家資金、流水返水
   - 後台為 SPA 架構，可直接用 URL 路徑跳轉

3. **VPS Puppeteer 環境建立**
   - VPS IP `128.199.249.195` 在後台白名單中（HTTP 200）
   - 安裝了 Chrome 所需的所有 Linux 底層庫（libatk, libnss3, libxfixes 等）
   - Puppeteer 在 VPS 上可正常執行（非 Docker 方式）

4. **Telegram 推播測試成功**
   - 抓取腳本 `/opt/pera57-reporting-bot/scrape.js` 已部署到 VPS
   - 成功抓取首頁數據（今日小結、平台概況）並格式化為繁體中文報告
   - 成功發送到 Telegram 群組 `P57回報機器人開發測試` (-5160138768)
   - Bot: `@pera57_report_bot` (token: `8764224944:AAHNTxmGsfCr77ct_wHaOWApg1Pe6q3vR2Q`)

5. **Signal 連結設備成功（但需重做）**
   - 韋瀚的 Signal 號碼: `+447427563920`（英國，私人使用）
   - 成功以 linked device 模式連接，測試訊息已送達
   - **但此號碼是韋瀚自己的，不能被機器人佔用，已解除連結**

### Signal 註冊問題總結
- **VPS 直接註冊失敗**：DigitalOcean IP 被 Signal 封鎖（502 ExternalServiceFailure）
- **墨西哥號碼 `+527711647956`**：403 Authorization Failed
- **英國號碼 `+447451253737`**：502 ExternalServiceFailure
- **Linked Device 模式可行**，但需要獨立的第二個 Signal 帳號
- **解決方案**：韋瀚需用備用手機以 `+447451253737` 註冊 Signal，再掃碼連結

### 待辦
- [ ] 韋瀚用備用手機註冊 `+447451253737` Signal 帳號
- [ ] 備用手機掃碼連結 VPS Signal API
- [ ] 機器人以 `+447451253737` 身份發送報告到韋瀚的 `+447427563920`
- [ ] 設置每小時自動排程（crontab）
- [ ] 擴充報告內容（十大玩家、十大遊戲、報表管理數據）
- [ ] 異常警報機制（大額提現、入金失敗率）

### 關鍵技術發現
- 後台有白名單限制，VPS IP 已在白名單內
- Signal API 容器在 VPS 健康運作（port 8081）
- Puppeteer 在 VPS 上直接跑（非 Docker），需安裝 ~10 個 Linux 圖形庫
- QR Code 連結有效期約 60-90 秒，需即時掃描

## Heartbeat 檢查 (17:41)
- **Token 消耗監控**：無異常大量消耗（各 session 使用率正常）
- **Anthropic OAuth 健康**：API key 正常，無錯誤計數
- **Puppeteer 殭屍清理**：無 `Chrome for Testing` 程序
- **Session 滿載檢查**：Discord channel 1490873797358911631 使用率 91%（超過 90%），建議監控或建立摘要
- **舊摘要清理**：無超過 7 天的舊摘要檔案
