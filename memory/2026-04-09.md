# 2026-04-09 Daily Log

## Q2 設定與配置

### MiniMax API 串接（Q2）✅
- 在 Q2 的 `openclaw.json` 中新增 MiniMax provider
- baseUrl: `https://api.minimax.io/v1`（官方新端點）
- API Key: 從 Q1 同步過來的
- 可用模型: `MiniMax-M2.5`, `MiniMax-M2.7`（204,800 context window）
- Q2 默認模型設為 `minimax/MiniMax-M2.5`
- Gateway 重啟成功，配置已生效

### Mac Sleep 設定更新
- 電池模式也改為 `sleep 0`（之前只有插電是 0）
- 現在螢幕打開時不論插電與否都不會自動休眠
- 闔上螢幕仍會正常休眠（硬體行為）

## Q1 修復紀錄

### Q1 Gateway 修復（下午）
- 根本問題：Node.js 不在 PATH 中，導致 `openclaw` 命令無法執行
- 修復：建立 `~/.npm-global/bin/openclaw` symlink，更新 `.zshrc` 加入 `/usr/local/bin` 和 `~/.npm-global/bin`
- Gateway 成功啟動

### Q1 反應慢問題修復
- 根因：Anthropic API 額度不足 → claude-opus-4-6 每次呼叫都失敗 → 觸發回退延遲
- 修復：
  - 默認模型改為 `google/gemini-3.1-pro-preview`
  - 回退鏈移除 anthropic，改為 `["deepseek/deepseek-reasoner", "openai/gpt-5"]`
  - Gateway 重啟成功
- AI Trading Bot 確認仍在正常運行（cron.log 有持續寫入）

### Q1 MiniMax 配置問題（待修正）
- Q1 用的 baseUrl 是 `api.minimaxi.chat`（舊域名），建議改為 `api.minimax.io`
- 模型 ID 大小寫問題：Q1 配的是 `minimax-m2.5`（小寫），官方是 `MiniMax-M2.5`（大寫 M）
- **Q1 SSH 連不上**：192.168.68.107 不通
- 韋瀚說 Q1 有用 VPN，IP 為 128.199.249.195，但那是 VPS 地址
- 需要韋瀚確認 Q1 的實際可達 IP（已請韋瀚在 Q1 上跑 `ifconfig`）

## LLM 模型比較表
- 為韋瀚製作了 2026 年 4 月更新版模型比較表
- 包含：Claude Opus 4.6 / Sonnet 4.6 / Gemini 2.5 Pro / Gemini 2.0 Flash / DeepSeek-V3.2 / GPT-4o
- 韋瀚指正初版資訊過舊，重新上網查證後更新
- 教訓：不要只靠內建知識，先搜尋最新資訊再回答

## 其他
- Q2 Discord Bot 頭像更新成功
- Q2 avatar 破圖問題修復（圖片複製到 workspace）
- 本 session token 使用率 83%，已建立摘要 `memory/session-summaries/main-webchat-q2.md`
- MiniMax API 在 Q1 上測試正常（兩個端點都能用）

## Q1 IP 變更
- 舊 IP: 192.168.68.107
- 新 IP: **192.168.68.105**（VPN tunnel: 172.16.9.1）
- MEMORY.md 中的 SSH 資訊需更新

## Q1 MiniMax 修復完成 ✅
- baseUrl: `api.minimaxi.chat` → `api.minimax.io`
- 模型 ID: `minimax-m2.5` → `MiniMax-M2.5`（大小寫修正）
- Gateway 重啟成功，RPC OK
