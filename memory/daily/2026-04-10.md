
## CS-Bot LiveChat Group 探索與品牌過濾（09:23-09:50）

### 探索結果
- **Group 0 = NASA11**（~20 個聊天）
- **Group 11 = PERA57**（25 個聊天）
- PERA57 bot ID: `aa5745c3c761782c321dcbca4a647693`
- NASA11 bot ID: `7da241b4fefaf635711d3ac731725411`
- Bot 是 Group 11 中 6 個聊天的成員

### 完成的修改
1. **poller.js**：`listChats({})` → `listChats({ group_ids: [11] })`，只監聽 PERA57
2. **handler.js**：`logToLcBuffer()` 添加 NASA11 內容過濾（雙重保障）
3. **docker-compose.yml**：添加 `./data:/app/data` volume mapping（之前 data 沒有持久化）
4. **LC buffer 回填**：從 API 拉取 Group 11 的 25 個歷史對話（212 條訊息），寫入 `lc-conversation-buffer.jsonl`
5. 驗證：buffer 中 0 條 NASA11 內容，46 條 PERA57 關鍵字

### 技術教訓
- shell 層面用 sed/heredoc 修改 JS 檔案容易出字符串引號問題
- 正確做法：用 `node -e` 在 host 上直接修改，或在容器內修改後 copy 出來
- `node -c file.js` 可以檢查語法
- LiveChat `listChats({ group_ids: [11] })` 可以有效過濾群組
