# 2026-04-02 Daily Log

## CS-Bot 專案重大進展

### LiveChat App 建立（終於搞定）
- **Dev Platform "Build App" UI 已被官方停用** — LiveChat 客服確認：沒建過 App 的帳號不能用此功能
- 繞過方式：透過 Bearer token 直接呼叫 `dev-platform.livechatinc.com/v2/applications` API
- Bearer token 從瀏覽器 XHR interceptor 擷取：`us-south1:FuSiP10Sa4M5wU5355wLOm_HZsI`
- App 建立成功（ID: `-8u8tvtDR` 等多個），但 authorization building block 無法透過 API 設定

### OAuth Client 建立
- 透過 Accounts API (`accounts.livechat.com/v2/clients`) 用 Bearer token 成功建立
- 最終使用的 client: `f6c49ee2be0a4175df750e4d0988a765`
- OAuth 授權流程完成（code exchange 成功），但 scope 為空
- 原因：scope 需在 Dev Platform 的 authorization building block 中設定，而 UI 壞了

### Bot Agent 建立成功
- 第一個 Bot（用第一個 client）：ID `aa5745c3c761782c321dcbca4a647693`
- 第二個 Bot（用新 client）：ID `c248cc3a84fc9aa24764adf71bdf92df`
- 目前使用第一個 Bot

### Webhook 困境 → 改用 Polling
- Webhook 已註冊（3 個），但需要 `enable_license_webhooks` 才能啟用
- `enable_license_webhooks` 要求 client_id 已「安裝」在 license 上
- 安裝需要 authorization building block（需要 UI 設定 scope）
- **結論：webhook 路線走不通，改用 PAT 輪詢方案**

### VPS 部署完成
- Docker container `pera57-cs-bot` 在 `128.199.249.195:3000` 運行中
- 輪詢模式，每 5 秒檢查新聊天/訊息
- 成功同步 25 個活躍聊天
- Health check: `http://128.199.249.195:3000/health`

### 檔案結構（VPS /opt/cs-bot/）
```
src/
  index.js      - Express server + polling startup
  poller.js     - LiveChat polling logic (list_chats 偵測新訊息)
  handler.js    - 處理新聊天/訊息的邏輯（目前只 log）
  livechat.js   - LiveChat API 呼叫（send_message, transfer_chat 等）
  telegram.js   - Telegram 通知
  logger.js     - JSON log
```

### 下一步
- [ ] 接 AI（GPT/Claude）做自動回覆
- [ ] 建 knowledge base 對接
- [ ] 實作升級邏輯（轉接真人 + Telegram 通知）
- [ ] 調整輪詢頻率避免 rate limit
- [ ] 測試實際客戶對話
